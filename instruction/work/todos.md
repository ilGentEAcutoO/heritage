# Active Tasks

> No active work — session shipped & archived.
> Last archived: 2026-06-17 → instruction/archive/011-ui-polish-and-warm-heritage/
>   (UI audit [85 findings] → Phase 0 + hero tree-canvas overhaul + shared UI kit/dialogs
>    + a11y sweep + app-wide dead-fallback purge + Warm Heritage visual re-theme — all live on prod)
> Prior: 10-security-review-and-frontend-hardening
> Everything live on https://heritage.jairukchan.com.
>
> ⏭️ DEFERRED to a future session — **layout / structural redesign** (the user's real ask).
>   The Warm Heritage work changed palette + typography but kept the same composition
>   (sidebar + tree + drawer, circular nodes), so it still "felt the same" to the user.
>   A true redesign needs a NEW layout / tree-visualisation, NOT a recolor. Blocked on a
>   reference / direction. Next time: get a reference app/screenshot, or mock up 2-3 distinct
>   LAYOUTS to pick from. Also optional: warm the alternate palettes, <Popover> primitive,
>   hero roving-tabindex, re-brand emails (still old sage #6b8f5e).
>
> To start new work: describe requirements → workflow-plan. To resume/check: "มีงานค้างไหม" → workflow-todo.

## File Lock Registry
| File | Locked by | Task | Since |
|------|-----------|------|-------|
| _(none)_ | | | |
